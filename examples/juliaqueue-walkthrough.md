# Juliaqueue Walkthrough

The fixture is intentionally compact, so the review starts with the cases that pull farthest apart.

| Case | Focus | Score | Lane |
| --- | --- | ---: | --- |
| baseline | input pressure | 118 | watch |
| stress | state drift | 211 | ship |
| edge | review cost | 172 | ship |
| recovery | decision risk | 180 | ship |
| stale | input pressure | 118 | watch |

Start with `stress` and `baseline`. They create the widest contrast in this repository's fixture set, which makes them better review anchors than the middle cases.

The useful comparison is `state drift` against `input pressure`, not the raw score alone.
